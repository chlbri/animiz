/** @jsxImportSource solid-js */

import type { Component } from 'solid-js';

type Props = {
  children: string;
};

export const NavLink: Component<Props> = ({ children }) => {
  return <div class=''>{children}</div>;
};
